# v2.8.0 真正雙頁導覽版

## 核心修正

先前版本把首頁與戰鬥介面放在同一份 HTML 中，只透過隱藏與顯示區塊切換。這會讓使用者誤以為按鈕沒有跳頁，也容易受到首頁排版、遮罩與狀態殘留影響。

本版改為真正的兩頁結構：

- `index.html`：首頁、領主選擇、難度選擇、規則、新手教學及資料來源。
- `battle.html`：換牌、對戰、回合結算、卡牌說明、組合技動畫及最終結果。

## 導覽行為

- 首頁按下「開始牌局」後，網址會切換至 `battle.html`。
- 選定的領主與電腦難度會透過網址參數帶入。
- 對戰頁載入後會立即建立牌局並顯示起手換牌。
- 「回主頁」會返回 `index.html`，而不是在同一頁顯示另一個區塊。
- 「重新開始」只重開目前對戰，不會返回首頁。

## 穩定性處理

- 首頁與對戰頁使用不同的 JavaScript 入口，避免首頁元件與戰鬥元件互相依賴。
- 對戰頁缺少首頁控制元件時，不會再引發空值錯誤。
- 開始按鈕本身是實際超連結，並由 JavaScript同步更新領主與難度參數。
