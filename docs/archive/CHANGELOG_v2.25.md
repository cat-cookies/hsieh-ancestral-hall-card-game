# v2.25

1. 每次進入或切換頁面時，字級重新依實際可視範圍自動校正，不再沿用上一頁的絕對百分比。
2. 校正條件納入 visualViewport、瀏覽器縮放造成的可視範圍變化、顯示器與系統縮放後的 CSS 視窗尺寸、直橫向、觸控裝置、短螢幕、注音版與英文版文字密度。
3. 滑桿仍可在當頁手動微調；按百分比按鈕可恢復自動校正。
4. 視窗縮放、手機轉向或網址列收合時會重新計算，手動微調則以相對比例保留。
5. 清除舊版跨頁絕對字級記錄，避免上一頁 200% 直接帶到下一頁。
